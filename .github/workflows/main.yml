name: macOS-Cloud-PC

on: 
  workflow_dispatch: # Allows you to start it manually from the Actions tab

jobs:
  build:
    runs-on: macos-latest
    steps:
      - name: Install & Configure ngrok
        run: |
          brew install ngrok
          ngrok authtoken ${{ secrets.NGROK_AUTH_TOKEN }}

      - name: Create VNC User & Enable Desktop
        run: |
          # 1. Create a fresh admin user 'vncuser' to bypass 'old password' prompts
          sudo dscl . -create /Users/vncuser
          sudo dscl . -create /Users/vncuser UserShell /bin/zsh
          sudo dscl . -create /Users/vncuser RealName "VNC User"
          sudo dscl . -create /Users/vncuser UniqueID 601
          sudo dscl . -create /Users/vncuser PrimaryGroupID 20
          sudo dscl . -create /Users/vncuser NFSHomeDirectory /Users/vncuser
          sudo dscl . -passwd /Users/vncuser "P@ssw0rd!"
          sudo dscl . -append /Groups/admin GroupMembership vncuser
          sudo mkdir -p /Users/vncuser
          sudo chown vncuser:staff /Users/vncuser

          # 2. Enable Apple's Native Screen Sharing (VNC)
          sudo /System/Library/CoreServices/RemoteManagement/ARDAgent.app/Contents/Resources/kickstart \
          -activate -configure -access -on \
          -clientopts -setvnclegacy -vnclegacy yes \
          -clientopts -setvncpw -vncpw "P@ssw0rd!" \
          -restart -agent -privs -all

          # 3. Grant full remote permissions to the new user
          sudo /System/Library/CoreServices/RemoteManagement/ARDAgent.app/Contents/Resources/kickstart \
          -configure -users vncuser -access -on -privs -all
          
          # 4. Prevent the system from sleeping
          sudo systemsetup -setdisplaysleep Off > /dev/null 2>&1 || true

      - name: Start ngrok Tunnel
        run: |
          # Tunneling the VNC port 5900
          ngrok tcp 5900 --log=stdout > ngrok.log &
          sleep 10

      - name: Get Connection Details
        run: |
          URL=$(curl -s http://localhost:4040/api/tunnels | jq -r '.tunnels[0].public_url')
          echo "------------------------------------------------------------"
          echo "Your Cloud Mac is Ready!"
          echo "VNC Address: $URL"
          echo "Username: vncuser"
          echo "Password: P@ssw0rd!"
          echo "------------------------------------------------------------"

      - name: Keep Alive
        run: |
          # Keeps the session active for up to 6 hours
          while true; do sleep 60; done
