name: Mac-VNC-Final

on: [push, workflow_dispatch]

jobs:
  build:
    runs-on: macos-latest
    steps:
      - name: Install & Configure ngrok
        run: |
          brew install ngrok
          ngrok authtoken ${{ secrets.NGROK_AUTH_TOKEN }}

      - name: Force Enable Screen Sharing (VNC)
        run: |
          # 1. Reset 'runner' password using the most basic command
          echo "P@ssw0rd!" | sudo passwd --stdin runner || sudo dscl . -passwd /Users/runner "P@ssw0rd!"
          
          # 2. Enable VNC with Legacy Password Support
          sudo /System/Library/CoreServices/RemoteManagement/ARDAgent.app/Contents/Resources/kickstart \
          -activate -configure -access -on \
          -clientopts -setvnclegacy -vnclegacy yes \
          -clientopts -setvncpw -vncpw "P@ssw0rd!" \
          -restart -agent -privs -all

          # 3. THE FIX for "Authentication Error": Force VNC to allow any client
          sudo defaults write /Library/Preferences/com.apple.VNCSettings.txt SecurityConfiguration -dict PrefsCauseSecurityUpgrade -bool false
          sudo defaults write /Library/Preferences/com.apple.RemoteManagement.plist VNCPassword -data $(echo "P@ssw0rd!" | perl -we 'print pack "H*", "1738515a454d3261"')

          # Give the system time to apply
          sleep 5

      - name: Start ngrok Tunnel
        run: |
          ngrok tcp 5900 --log=stdout > ngrok.log &
          sleep 10

      - name: Get VNC Address
        run: |
          URL=$(curl -s http://localhost:4040/api/tunnels | jq -r '.tunnels[0].public_url')
          echo "------------------------------------------------------------"
          echo "Your VNC Address: $URL"
          echo "Username: runner"
          echo "Password: P@ssw0rd!"
          echo "------------------------------------------------------------"

      - name: Keep Alive
        run: |
          while true; do sleep 60; done
